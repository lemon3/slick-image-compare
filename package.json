{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "docs": "pnpm --filter slick-image-compare build --config vite.config.pages.js",
    "build": "pnpm --recursive run build",
    "build:core": "pnpm --filter slick-image-compare build",
    "build:react": "pnpm --filter slick-image-compare/react build"
  },
  "devDependencies": {
    "slick-image-compare": "workspace:*"
  }
}
